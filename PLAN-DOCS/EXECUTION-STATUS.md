# Execution status — event-test sequence (F0.1, F0.2, UX-TESTS, S5, S2)

Generated 2026-08-01 on branch `phase4-builder-harness`, repo `/home/unix/bubblelab-suite`.

## Task table

| Task                                            | Status | Tree green                  | Files touched                                                                                                                                                                                                                                                   | Verified by                                                                                                                                                                                                                                                                                                                    | Remaining                                                                                                                                                                                                                                         |
| ----------------------------------------------- | ------ | --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **F0.1** — event-test harness                   | done   | yes                         | `scripts/event-test/`: `package.json`, `.gitignore`, `harness.mjs`, `run.mjs`, `lib/{stack,api,signals,sources,fixtures,browser,report}.mjs`, `tests/_smoke.test.mjs`, `tests/_snag_red.test.mjs` (13 files)                                                    | `run.mjs tests/_smoke.test.mjs` exit 0, 14/14 PASS incl. negative paths (red child exit 1 + valid report; dead-stack exit 3, `assertions:[]`); `_snag_red` exit 1 expected red with structured report; `node --check` on all 11 .mjs PASS; api `npx tsc --noEmit` exit 0; `/telemetry?type=event_test.run` shows 3 self-events | PR (blocked on F0.3 gate). Keep `lib/signals.mjs` in sync with `apps/bubble-studio/src/utils/executionErrorSignals.ts` in the same PR whenever it changes. Exclude `_snag_red` from green gates.                                                  |
| **F0.2** — pr-on-green hook                     | done   | yes                         | `scripts/event-test/pr-on-green.mjs`, `scripts/event-test/tests/_pr_hook.test.mjs`, `PLAN-DOCS/pr-bodies/F0.2.md`, `BACKLOG.md`                                                                                                                                 | `node --check` both files PASS; `run.mjs tests/_pr_hook.test.mjs` exit 0, 22/22 (red / green / guard paths + 2x `pr_on_green.hook_run` self-events); `gh pr list` confirms 0 PRs opened (F0.3 gate honored)                                                                                                                    | PR (blocked on F0.3). One real non-dry-run invocation on a throwaway branch (FND acceptance step 4) after F0.3 clears. Authored PR-body files must not contain literal `## How verified` / `## Failing event tests` headings.                     |
| **UX-TESTS** — U-1/U-3/U-4/U1/U2/U3 event tests | done   | yes                         | `scripts/event-test/lib/{studio,uxFixtures}.mjs`, `tests/{u-1_credential_request,u-3_drag_persist,u-4_edge_labels,u1_curated_view,u2_result_surfaces,u3_overflow}.test.mjs`                                                                                     | Runner exit 0 on all six (fast 4/4; U-1 6/6; U2 16/16) against live stack api:3100/sidecar:3101/studio:3102; typecheck: bubble-studio tsc exit 0, bubblelab-api tsc exit 0, builder-agent typecheck exit 0                                                                                                                     | Per-feature PRs (blocked on F0.3): one branch/PR per feature, update BACKLOG rows; U-3/U-4 PRs add `canvas.*` telemetry so those tests graduate off the browser bridge. U-1 fixture leaves its build thread in `blocked_on_credential` by design. |
| **S5** — error identity signals                 | done   | yes                         | `apps/bubble-studio/src/utils/executionErrorSignals.ts` (+ `.test.ts`), `apps/bubble-studio/src/components/execution_logs/AllEventsView.tsx`, `services/builder-agent/src/prompts.ts`, `scripts/event-test/lib/signals.mjs`, `tests/s5_error_identity.test.mjs` | vitest `executionErrorSignals.test.ts` 14/14 PASS; bubble-studio tsc exit 0; builder-agent typecheck exit 0; `run.mjs tests/s5_error_identity.test.mjs` exit 0 (distinct per-step signals `http#412` 404 vs `http#413` 410); `_smoke` regression exit 0                                                                        | PR (blocked on F0.3). Optional: S1 emit-layer fix (thread real variableName), slow-tier live one-turn-two-fixes Gluu proof, re-check external `gluu-fix-test.mjs` for old message text.                                                           |
| **S2** — runtime lint rules                     | done   | source truncated (see note) | `packages/bubble-runtime` validation files — full list truncated in source data                                                                                                                                                                                 | bubble-runtime `npx tsc --noEmit` exit 0; `npm run build` PASS; `lint-rules.test.ts` 68/68 PASS (12 new S2 cases); `index.test.ts` 22 pass + 4 new S2 pass, 1 FAIL ("should validate yfinance flow") confirmed PRE-EXISTING on base via git stash + rerun; further verification detail truncated in source data                | PR (blocked on F0.3). Re-derive full filesTouched/deviations from the S2 task session; the handoff JSON was cut mid-record.                                                                                                                       |

## Key deviations (surfaced per the deviation clause)

- **F0.1**: `lib/stack.mjs` treats the HTTP health probe as liveness authority (pidfile pids were dead under supervisor respawn; pidfile supplies ports only). Persisted `items[].result` is the runtime wrapper `{data, errors, warnings, ...}`; its `data` equals streamed `execution_complete.additionalData.result`. `_snag_red` added beyond the FND file list.
- **F0.2**: Real-PR acceptance step deliberately skipped (brief + F0.3 gate forbid it); dry-run does not enforce preconditions (real mode does, exit 2). BACKLOG F0.2 row set to LOCAL, not "In review".
- **UX-TESTS**: U-3/U-4 assert structured page state via the sanctioned `lib/browser.mjs` bridge because no logged event exists yet (graduation = `canvas.node_position_persisted` / `canvas.edges_built` telemetry in their PRs). Mid-task stale-API restart (orphan pids serving stale code) resolved via `ss -ltnp` + `dev-stack.sh up`; no code change.
- **S5**: Live Gluu-turn proof excluded from the automated test (1-5 min nondeterministic); the composed fix message is asserted instead. Local stub HTTP server replaces httpbin. Full identity string ships in the visible Errors panel (F0.5 friendly split deferred).

## PR status

**Every task above still needs its PR.** All are blocked on the F0.3 GCloud registration / branching gate; work is local on `phase4-builder-harness`. Once F0.3 clears: one branch/PR per task via `scripts/event-test/pr-on-green.mjs`, each consuming `scripts/event-test/.reports/latest.json`, with BACKLOG.md row updates riding each PR (Pillar 6).

## URGENT flags

None. Every task that reported `treeGreen` reported `true`, and each backed it with an exit-0 typecheck plus passing event tests. S2's tree-green field arrived truncated, but its reported tsc exit 0 + build PASS + the single failure being confirmed pre-existing on base leave no evidence of a non-green tree. The final combined check below confirms the shared studio surface (touched by S5 and UX-TESTS) compiles clean.

## Final combined check

```
cd /home/unix/bubblelab-suite/apps/bubble-studio && npx tsc --noEmit -p tsconfig.json
```

Exit code: **0** (run 2026-08-01, post-all-tasks working tree).
